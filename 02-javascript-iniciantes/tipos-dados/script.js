//Exercicios

// Declare uma varivel contendo uma string

  var esporte = 'Futebol';
  console.log(esporte);

// Declare uma varivel contendo um numero dentro de uma string

 var ano = '2020';
 console.log(ano);

// Declare uma variavel com sua idade

  var idade = 23;
  console.log(idade);

// Declare duas variaves, uma com seu nome e outra com seu sobrenome e some as mesmas

  var nome = 'Gustavo ';
  var sobreNome = 'Castro';
  var nomeCompleto = nome + sobreNome;
  console.log(nomeCompleto);

// Coloque a seguinte frase em uma variavel "It`s time"

  var frase = `It's time`;
  console.log(frase);

// Verifique o tipo de variavel que contem seu nome
  var verificaTipoNome = typeof nome;
  console.log(verificaTipoNome);